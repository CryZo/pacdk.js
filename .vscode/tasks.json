{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run App (Dev)",
      "type": "shell",
      "group": "build",
      "command": "docker compose up --build",
      "isBackground": true,
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
